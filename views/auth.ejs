<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= isRegisterPath ? 'Register' : 'Login' %>
    </title>
    <!-- Tailwind CSS cdn -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- fontawsome cdn -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body class="bg-gray-100 h-screen flex items-center justify-center">

    <!-- Back to Home Link -->
    <div class="absolute top-5 left-0 w-full flex justify-center">
        <a href="/" class="flex items-center text-gray-700 hover:text-yellow-500">

            <i class="fa-solid fa-angle-down w-5 h-5 mr-1 mt-2"></i>
            <span>Back to Home</span>
        </a>
    </div>

    <div class="auth-container flex justify-center items-center w-full">
        <div class="lg:w-2/6">
            <div class="auth-form p-8 shadow-lg bg-white ">
                <img src="https://cdn-icons-png.flaticon.com/512/309/309492.png" alt="no image"
                    class="mx-auto mb-4 w-20">
                <h3 class="text-2xl font-bold text-center mb-4">Welcome <%= isRegisterPath ? 'User' : 'Back' %>
                </h3>

                <!-- Login or Register Form -->
                <form action="<%= isRegisterPath ? '/register' : '/login' %>" method="POST">
                    <% if (error) { %>
                        <div class="error-message text-red-600">
                            <%= error %>
                        </div>
                        <% } %>

                            <% if (isRegisterPath) { %>
                                <div class="input-group mb-4">
                                    <label for="username" class="block text-gray-700 font-semibold">Name</label>
                                    <input type="text" name="username" id="username" required
                                        placeholder="Enter your name"
                                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500">
                                </div>
                                <% } %>

                                    <div class="input-group mb-4">
                                        <label for="email" class="block text-gray-700 font-semibold">Email</label>
                                        <input type="email" name="email" id="email" required
                                            placeholder="Enter your email"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500">
                                    </div>

                                    <div class="input-group mb-6">
                                        <label for="password" class="block text-gray-700 font-semibold">Password</label>
                                        <input type="password" name="password" id="password" required
                                            placeholder="Enter your password"
                                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500">
                                    </div>

                                    <div class="flex justify-center mb-6">
                                        <button type="submit"
                                            class="w-full py-2 bg-yellow-500 text-white rounded-lg hover:bg-yellow-600 transition duration-300 ease-in-out">
                                            <%= isRegisterPath ? 'Sign Up' : 'Login' %>
                                        </button>
                                    </div>

                                    <p class="text-center text-gray-600">
                                        <% if (isRegisterPath) { %>
                                            Already have an account? <a href="/login"
                                                class="text-yellow-500 hover:underline">Login</a>
                                            <% } else { %>
                                                Don't have an account? <a href="/register"
                                                    class="text-yellow-500 hover:underline">Sign Up</a>
                                                <% } %>
                                    </p>
                </form>
            </div>
        </div>
    </div>

</body>

</html>